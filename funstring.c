#include<stdio.h>
void main(){
    int count,i,n=100,len;
    char b[100];
    scanf("%[^\n]s",&b);
    printf("character is:%s\n",b);
    //for(i=0;b[i] !=  NULL;i++){
    {
            strlen(b);
           len= strlen(b);
        }
printf("%d",len);

    }
