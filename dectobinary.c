#include<stdio.h>
int main(){
    int num,i,num1,num2,c;
    printf("enter the number");
    scanf("%d",&num);
   for(i=0;i=1;i++){
        if(num%2==0){
        printf("%d==0",num==0);
        int num1=num1+num;
        }
        else if(num%2>=1){
        printf("%d==1",num==1);
        int num2=num2+num;
        }
   num =num1, num2;
  c=(num==0)? printf("%d==0",num):(num>=1)? printf("%d==1",num):
        printf("the decimal value is %d",num);
   }

        return 0;
        }
