#include<stdio.h>
void main(){
    int count,i,n=100;
    char b[100];
    scanf("%[^\n]s",&b);
    printf("character is:%s\n",b);
    for(i=0;b[i] !=  NULL;i++){
    {
            count ++ ;
        }}
        printf("the length of character:%d",count);

    }
