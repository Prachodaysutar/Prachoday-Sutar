#include<stdio.h>
int main()
{   int count;
    int i,a[4]={1,1,3,4},b[i];
    for(i=0;i<4;i++){
            printf("elements of a is %d\n",a[i]);
    a[i]=b[i];
    }
      count =0;
    for(i=0;i<=4;i++){
        if(a[i]=b[i]){
            count ++;

        }
        printf("dublicate num is %d\n",count);
    }

    return 0;
}
